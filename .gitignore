__pycache__/
*.pyc
.DS_Store
.claude/
.superpowers/
.worktrees/
.playwright-cli/
*.mcpb
dist/
*.egg-info/
# codetalk 本地产物防护网:正常写在 repo 外的 ~/(cache/usage/vault);此处防误配
# (如 --vault . / 在仓内跑样本)时产物意外入库泄露。样本仓数据一律只进隔离临时目录。
.vibetrace/
.codetalk/
codetalk-reports/
*-graph.html
*-graph.canvas
# docs 下的渲染产物(如问卷汇报导出);源码模板在 codetalk/ 不受影响
docs/**/*.html
/tmp_codetalk/
# 本地 IDE / MCP 配置(可能含密钥或本地上下文)——永不入库
.cursor/
.idea/
.mcp.json
# VS Code extension build artifacts
vscode-codetalk/node_modules/
*.vsix
# 内部战略/设计文档不入公开仓(2026-07-01 翻公开前 filter-repo 抹除历史;本地保留供 skill/briefing)
# 仅放行:interceptions(产品产物;护城河对照卡已移到 docs/moat-real-records-vs-inference.md,不在此目录)
docs/discovery/
!docs/discovery/interceptions.md
docs/superpowers/
/ROADMAP.md
# Local private workspace; never publish.
/.private/
