name: test

on:
  push:
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]   # 覆盖 requires-python >=3.11 全支持面
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0          # full history: --history secret scan needs every commit
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package
        run: python -m pip install -e ".[web]"
      - name: Scan repository files for secrets
        run: python -m scripts.scan_secrets --history
      - name: Run tests
        run: python -m unittest discover -s tests

  vscode-extension:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-codetalk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: vscode-codetalk/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Prepare package license
        run: npm run prepackage
      - name: Verify package license
        run: test -f LICENSE
      - name: Typecheck
        run: npm run typecheck
      - name: Build
        run: npm run build
