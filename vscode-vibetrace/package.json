{
  "name": "vscode-vibetrace",
  "displayName": "vibetrace — Decision Blame",
  "description": "See why code was written this way, not just who/when. Foldable CodeLens with real commit decisions, hover cards with full context.",
  "version": "0.2.0",
  "publisher": "vibetrace",
  "license": "AGPL-3.0-or-later",
  "repository": { "type": "git", "url": "https://github.com/HUKAIR/CodeTalk" },
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      { "command": "vibetrace.expandAll", "title": "vibetrace: Expand All Decisions" },
      { "command": "vibetrace.collapseAll", "title": "vibetrace: Collapse All Decisions" }
    ],
    "configuration": {
      "title": "vibetrace",
      "properties": {
        "vibetrace.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable inline decision annotations"
        },
        "vibetrace.pythonPath": {
          "type": "string",
          "default": "python3",
          "description": "Path to Python interpreter with vibetrace installed"
        }
      }
    }
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.4.0"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit",
    "watch": "node esbuild.mjs --watch",
    "package": "npx @vscode/vsce package --no-dependencies"
  }
}
